
import gc
import led
import sys
import machine
import api
import sendData as db
import machine

telegram = api.TelegramBot('964820596AAHFec_OfTebXv4TuboSRFQ-Bl9rGiWY5Nk')
print('!!!1s1!!!')
button = machine.Pin(2, machine.Pin.IN, machine.Pin.PULL_UP)
print(button.value())

if button.value() == 0
        print('done EXIT')
        led.blink()
        #telegram.send(message[0], 'exit.....')
        print(message[2])
        sys.exit()

def message_handler(message)
  
    
  
    if message[2].isdigit()
        led.blink()
        telegram.send(message[0], 'received  digit = ' + message[2])
        print(message[2])
        db.send(message[2])
    
    else
      if message[2] == 'exit'
          led.blink()
          telegram.send(message[0], 'exit.....')
          print(message[2])
          sys.exit()
          #machine.reset()
      else
          led.blink()
          telegram.send(message[0], 'received  text = ' + message[2])
          print(message[2])
        

          
    gc.collect()
        
telegram.listen(message_handler)

